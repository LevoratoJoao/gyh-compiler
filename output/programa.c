#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <stdbool.h>

int main() {
	float Parametro2;
	int Parametro;
	int Fatorial;

	Fatorial = 3 / 3;
	if (Parametro == 1) {
		if (Parametro >= 1) {
		Fatorial = Parametro;
	}
		Fatorial = Fatorial * Parametro + 1;
		while (Parametro == 1 && Parametro < 1) {
		Fatorial = Parametro;
		Fatorial = Fatorial * Parametro + 1;
	}
	}
	else {
		printf("%d\n", Fatorial);
	}
	printf("%d\n", Parametro);
	Parametro = Parametro + 1;
	return 0; 
}